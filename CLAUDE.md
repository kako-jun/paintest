# paintest — 開発ガイド

## コンセプト

Windows 11 の Paint 相当のクラシックツールに加え、**Photoshopでよく使う機能（レイヤー・トーンカーブ・柔軟な範囲選択・色調補正・レイヤーの変形）** を再現し、日常的な画像編集でPhotoshopを開かずに済むmacOSネイティブアプリにする。基本ツールはUIの見た目の忠実さより**ドット単位でぼけない編集**を最優先する。

- バケツ（実装済み#38）・グラデーション（実装済み#41）・消しゴム・図形・スポイト・テキスト等のクラシックツールはアンチエイリアスなしでピクセルを直接編集する（**テキストのみ例外**: フォントのグリフ描画はAppKitのテキスト描画仕様上アンチエイリアスを完全に排除できない。詳細は「既知の制約」参照）
- **鉛筆（にじみなし・ドット単位・アンチエイリアスなし）とペン（にじみあり・アンチエイリアスあり）を別ツールとして使い分ける**。既存の「ブラシ」はペンに統合済み（`ToolboxView`の`paintbrush.fill`アイコンは`tool: .pen`）で別ツールとしては存在しない（#10）
- ズームは常にニアレストネイバー（`CGContext.interpolationQuality = .none`）。拡大してもドットが滲まない
- レイヤー（重ね合わせ・表示切替・並べ替え・変形・ブレンドモード＝乗算/スクリーン/オーバーレイ等、実装済み#37・結合＝下のレイヤーと統合／画像を統合、実装済み#40）、トーンカーブ・色調補正（明るさ/コントラスト・色相/彩度・レベル補正含む、実装済み#12。選択範囲があればその範囲のみに適用、`ImageAdjustments.swift`）、柔軟な範囲選択（矩形/楕円/投げ縄/多角形/マジックワンド・追加/削除/交差/反転、実装済み#11）、画像解像度／カンバスサイズの変更（実装済み#39）はPhotoshop相当機能として対象内（旧「クラシックPaint相当スコープ」からの方針転換。旧方針はREADME/CLAUDE.mdの過去版を参照）
- AI背景除去等の生成AI機能は引き続き対象外
- UIは**レイアウト構成**（ツールバー・レイヤーパネル・プロパティパネル・オプションバー等の画面上の区画配置と操作導線）をPhotoshopに寄せる。配色・アイコンデザインは独自でよい（ピクセル単位の一致は不要）
- **複数ドキュメントを同時に開ける**。タブUIはPhotoshopの上部横タブではなく、**Microsoft Edgeのような縦タブ帯を左側**に配置する。各ドキュメントは独立した`LayerStack`とズーム倍率を持つ
- Web wrapper（Tauri/Electron等）ではなく、Swift/AppKitによる純粋なmacOSネイティブアプリとする

## 技術スタック

- **言語**: Swift
- **UI**: AppKit（`NSWindow` / `NSView`ベース。SwiftUIは補助的に検討可）
- **描画**: Core Graphics（`CGContext`）
- **第一対応 OS**: macOS
- **保存形式**: PNG（フラット統合出力、将来的にBMP検討）に加え、レイヤー構造を保持する独自パッケージ形式`.paintestdoc`（ディレクトリ+`manifest.json`+レイヤーごとのPNG）

## ローカル起動

```bash
swift build
swift run
```

## テスト

```bash
swift test
```

ロジック本体は`paintestCore`（ライブラリターゲット）に置き、`paintest`（実行ターゲット）は起動のみを担う。XCTestが実行ターゲットを直接importできないための分割。UI依存（`NSAlert.runModal`・`NSEvent`・実描画）で単体テスト不可能な部分は無理にテスト化せず、実機確認に委ねる。

## 実装方針

- キャンバスの内部解像度（ピクセルグリッド）とビュー表示サイズを分離し、拡大縮小は常に整数倍・ニアレストネイバーで行う
- `CGContext.interpolationQuality = .none` を全描画パスで徹底し、サブピクセル座標の丸め誤差でアンチエイリアスがかかる経路を作らない
- クラシックツールはWindows Paintのツールバー構成・アイコン配置に寄せるが、ピクセル寸法まで一致させることは目的にしない
- レイヤー・トーンカーブ・柔軟な範囲選択・色調補正・レイヤー変形はPhotoshopの画面レイアウト（左ツールバー・上部オプションバー・右側レイヤー/プロパティパネル等の区画配置）に寄せる。配色・アイコンは独自路線

## 既知の制約

- **非normalブレンドモードが絡むレイヤー結合／画像の統合は近似結果になる場合がある**（issue #40自己レビューmust-2、kako-jun判断: 厳密な合成アルゴリズムへの作り直しはせず現状維持）。`LayerStack.mergeDown`/`flatten`は2枚のレイヤーを孤立した透明キャンバスへ描画してから1枚に焼き込むため、上側レイヤーが半透明かつ乗算・スクリーン・オーバーレイ等の非normalブレンドモードが絡むケースでは、結合後の1枚のレイヤー＋1つのblendModeでは元の見た目をさらに下のレイヤーとの関係まで含めて数式的に完全再現できないことがある。Photoshop等の一般的な画像編集ソフトも「統合」機能で同種の制約を持つ。詳細は`LayerStack.mergeDown`のdocコメントを参照
- **テキストツールのグリフ描画はアンチエイリアスを完全には排除できない**（issue #42）。`CanvasView.rasterizeText(_:at:)`は、`textSettings.fontSize`そのままのピクセル寸法で確保した`NSBitmapImageRep`へ`NSAttributedString.draw(with:options:)`でテキストを描き、得られた`CGImage`を`PixelCanvas.compositeImage(_:at:mask:)`でレイヤーへ合成する。AppKitのグリフ描画自体はアンチエイリアスがかかるため、鉛筆・図形描画のような`setPixel`/`drawLine`ベースの非AA化はできない。`compositeImage`は`CGImage`を`NSBitmapImageRep`として読み戻し、元画像の各ピクセルを合成先へ1:1でsource-over合成するだけなので、拡大縮小や`CGContext.draw`は使わず、`interpolationQuality`もこの経路では結果に影響しない。グリフ縁に元々かかっているアンチエイリアシングは残る。詳細は`CanvasView.rasterizeText(_:at:)`のdocコメントを参照

## リポジトリ方針

- `README.md` はエンドユーザー向け。実装・設計判断は `docs/` とこのファイルに書く
- `backend/`相当は置かず、Swift Package標準構成にする（`paintestCore`ライブラリ + `paintest`実行 + `paintestCoreTests`テストの3ターゲット）

## 禁止事項

- Co-Authored-By をコミットメッセージに付けない
- 鉛筆・図形描画にアンチエイリアス/ぼかしを混ぜない（ペンツールはにじみあり=アンチエイリアス前提のため対象外）
- Tauri/Electron等のWeb wrapperに戻さない（ネイティブ方針で確定済み）
